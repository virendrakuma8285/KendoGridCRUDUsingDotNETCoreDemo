import { AfterContentInit, OnDestroy, ElementRef, ChangeDetectorRef, Renderer2 } from '@angular/core';
/**
 * Represents the Kendo UI TextBox component for Angular.
 *
 * Provides floating labels to `input` elements.
 *
 * @example
 * ```ng-template
 * <kendo-textbox-container floatingLabel="First name">
 *   <input kendoTextBox />
 * </kendo-textbox-container>
 * ```
 */
export declare class TextBoxContainerComponent implements AfterContentInit, OnDestroy {
    private elementRef;
    private renderer;
    private changeDetectorRef;
    readonly hostClasses: boolean;
    readonly focusedClass: boolean;
    readonly emptyClass: boolean;
    /**
     * Sets the `id` attribute of the input inside the container.
     */
    id: string;
    /**
     * Adds a floating label that describes the input.
     */
    floatingLabel: string;
    textbox: ElementRef;
    /**
     * @hidden
     */
    focused: boolean;
    /**
     * @hidden
     */
    empty: boolean;
    private _listeners;
    constructor(elementRef: ElementRef, renderer: Renderer2, changeDetectorRef: ChangeDetectorRef);
    /**
     * @hidden
     */
    ngAfterContentInit(): void;
    /**
     * @hidden
     */
    ngOnDestroy(): void;
    /**
     * @hidden
     */
    private listen(element, eventName, handler);
}
